# 覆盖层

模态遮罩（`.overlay`）用于覆盖当前容器，配合（`.overlay-blocker`）阻止用户与背景内容交互，常用于配合对话框、加载等场景。

## 基本用法

```html
<div style="width:300px; height:300px; position: relative;">
    <div class="overlay"></div>
</div>
```
